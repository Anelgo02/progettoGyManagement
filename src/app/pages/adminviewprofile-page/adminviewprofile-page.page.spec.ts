import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AdminviewprofilePagePage } from './adminviewprofile-page.page';

describe('AdminviewprofilePagePage', () => {
  let component: AdminviewprofilePagePage;
  let fixture: ComponentFixture<AdminviewprofilePagePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AdminviewprofilePagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
